# Instructions for Toy MLP Experiment on MNIST

We have provided weights from a single run in "weights/" folder.

To reproduce training and evaluation, run:
```
sh mlp_exp.sh
```

Each run takes approximately 30min to 1 hour.